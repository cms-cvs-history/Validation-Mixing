
process PRODVAL2  = {

	source = PoolSource { 
		untracked vstring fileNames = {"file:/tmp/Cum_xxx.root"}
	}

        untracked PSet maxEvents = {untracked int32 input = 1}

	module test = TestSuite {
		int32 BunchNr = xxx
		int32 minBunch = bcrs
		int32 maxBunch = bcre
		string fileName = "histos.root"
	}
	path p = { test}

        service = DaqMonitorROOTBackEnd{ }
}
