
process PRODVAL2  = {

        service = DaqMonitorROOTBackEnd{ }

	source = PoolSource { 
		untracked vstring fileNames = {"file:/tmp/Cum_global.root"}
	}
        untracked PSet maxEvents = {untracked int32 input = 10}

	module test = GlobalTest {
                int32 minBunch = -5
                int32 maxBunch = 3
                string fileName = "GlobalHistos.root"
	}

	path p = { test}
}
